╔══════════════════════════════════════════════════════════════╗
║          Installation Guide - Step by Step                ║
╚══════════════════════════════════════════════════════════════╝

📦 WHAT NEEDS TO BE INSTALLED:
───────────────────────────────

The package requires Python and several libraries. Everything can be
installed automatically using pip - no manual downloads needed!


✅ AUTOMATIC INSTALLATION (Recommended):
───────────────────────────────────────

1. Open terminal/command prompt in this folder

2. Install all dependencies:
   
   pip install -r requirements.txt

   ⚠️  First-time installation takes 5-10 minutes
   ⚠️  Requires internet connection (downloads AI models)
   ⚠️  Downloads ~1-2GB of AI models on first run


✅ WHAT GETS INSTALLED:
───────────────────────

The requirements.txt will automatically install:

   ✓ deepface         →  Face recognition library (includes TensorFlow)
   ✓ opencv-python    →  Camera and image processing
   ✓ numpy            →  Mathematical operations
   ✓ pandas           →  Data handling
   ✓ Pillow           →  Image processing
   ✓ tqdm             →  Progress bars

   DeepFace will also install:
   ✓ tensorflow       →  Deep learning framework (large download)
   ✓ keras            →  Neural network library
   ✓ gdown            →  Model downloading
   ✓ flask            →  Web framework (if needed)
   ✓ matplotlib       →  Plotting utilities

────────────────────────────────────────────────────────────────

⚠️  SYSTEM REQUIREMENTS:
─────────────────────────

✓ Python 3.7 or higher (Python 3.8+ recommended)
✓ 4GB RAM minimum (8GB recommended)
✓ ~5GB free disk space (for models and packages)
✓ Internet connection (first installation only)
✓ Webcam/Camera

────────────────────────────────────────────────────────────────

🖥️  PLATFORM-SPECIFIC NOTES:
─────────────────────────────

WINDOWS:
   • Usually works out of the box
   • May need Microsoft Visual C++ Redistributable
   • If errors, install: pip install --upgrade pip setuptools wheel

LINUX (Ubuntu/Debian):
   • May need system packages:
     sudo apt-get update
     sudo apt-get install python3-dev python3-pip
     sudo apt-get install libgl1-mesa-glx libglib2.0-0
   
   • For better performance:
     sudo apt-get install libgtk-3-dev

MACOS:
   • Works with Python 3.7+
   • May need Xcode Command Line Tools:
     xcode-select --install

────────────────────────────────────────────────────────────────

🚀 VERIFY INSTALLATION:
───────────────────────

After installation, verify everything works:

   python -c "import deepface; print('DeepFace OK')"
   python -c "import cv2; print('OpenCV OK')"
   python -c "import tensorflow; print('TensorFlow OK')"

If all print "OK", you're ready to go!

────────────────────────────────────────────────────────────────

❌ TROUBLESHOOTING:
──────────────────

Problem: "pip is not recognized"
   → Install Python from python.org
   → Make sure to check "Add Python to PATH" during installation

Problem: Installation fails with TensorFlow errors
   → Try: pip install --upgrade pip
   → Then: pip install -r requirements.txt

Problem: "No module named 'cv2'"
   → Reinstall: pip install opencv-python

Problem: Out of disk space
   → DeepFace downloads ~1-2GB of models
   → Free up space or use cloud storage

Problem: Slow installation
   → Normal - TensorFlow is large (~500MB)
   → Be patient, it's a one-time download

────────────────────────────────────────────────────────────────

✅ ONCE INSTALLED:
──────────────────

After successful installation, you only need to:
   1. Add face images to database/ folder
   2. Run: python advanced_realtime_recognition.py

No further installation needed! Models download automatically on first run.

═══════════════════════════════════════════════════════════════

